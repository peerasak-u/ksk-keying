# secrets
.env
.claude/skills/ksk-keying/scripts/.env

# tool run cache / outputs
.claude/skills/ksk-keying/scripts/.runs/
dist/

# client data — never commit real client folders
samples/
.claude/skills/ksk-keying/scripts/samples/

# deps / os
node_modules/
.DS_Store

# ksk console runtime state (never commit)
console/runs/
console/demo-workspace/
# The container's own ~/.claude.json (see docker-compose.yml) — seeded from the
# operator's real file, so it carries their account identity. Never commit.
console/state/
