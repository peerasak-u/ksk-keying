{
  "name": "ksk-console",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i public/style.src.css -o public/style.css --minify",
    "app": "bun run app/server.ts",
    "proto:excluded-review": "bun run _prototype_excluded_review/serve.ts"
  },
  "devDependencies": {
    "tailwindcss": "^4.3.3",
    "@tailwindcss/cli": "^4.3.3"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
